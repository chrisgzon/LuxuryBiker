export const environment = {
  production: true,
  // Vacío = mismo origen (la SPA se sirve desde el wwwroot de la API).
  // Ajustar si el frontend y el backend se despliegan en dominios distintos.
  apiUrl: '',
};
